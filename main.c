#include <stdio.h>
#include <stdlib.h>
#include "secretCoder.h"

int main()
{
    //printf("%s", encoded);
    //char *encrypted;
    char *keyFile = "songLibrary/sweetChildGR.txt";
    encrypted = encode(messageFile, keyFile);
}
